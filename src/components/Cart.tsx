import {Games } from "../types/types";

export const Cart = ({ }) => {
  return (
    <>
      {(() => (
        <div className="bg-white p-6 shadow-slate-500 m-5">
  
        </div>
      ))}
    </>
  );
};
