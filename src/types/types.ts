import { ReactNode } from "react";

export type game = {
  email: ReactNode;
  phone: ReactNode;
  address: ReactNode;

  id: number;
  name: string;
  description: string;
  price: number;
  image: string;
  item: string;
  contacts: string;
  about: string;
};

export type  description = {
  id: number;
  name: string;
  description: string;
  price: number;
  image: string;
  item: string;
  contacts: string;
  about: string;
};


